// SPDX-License-Identifier: UNLICENSE
pragma solidity ^0.8.26;

import { BlueprintServiceManagerBase } from "@tnt-core/BlueprintServiceManagerBase.sol";

/**
 * @title ExperimentalBlueprint
 * @dev This contract is a minimal example of a BSM that uses all defaults.
 * @dev For a comprehensive example with hook tracking, see IncredibleSquaringBSM.sol
 * @dev For all supported hooks, check the `BlueprintServiceManagerBase` contract.
 */
contract ExperimentalBlueprint is BlueprintServiceManagerBase {
    // This contract uses all default implementations from BlueprintServiceManagerBase.
    // Override any hooks you need to customize.
}
