[package]
name = "blueprint-client-tangle"
version = "0.2.0-alpha.4"
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Tangle client for Blueprint SDK - connects to Tangle EVM contracts"
readme = "README.md"
publish = true

[lints]
workspace = true

[dependencies]
# Workspace
blueprint-core.workspace = true
blueprint-std.workspace = true
blueprint-client-core.workspace = true
blueprint-keystore = { workspace = true, default-features = false }
blueprint-crypto = { workspace = true, default-features = false, features = ["k256"] }
url = { workspace = true, features = ["serde"] }
k256 = { workspace = true }
tnt-core-bindings.workspace = true

# Alloy (EVM interaction)
alloy-primitives = { workspace = true, features = ["serde"] }
alloy-sol-types = { workspace = true }
alloy-provider = { workspace = true }
alloy-transport = { workspace = true }
alloy-rpc-types = { workspace = true, features = ["eth"] }
alloy-contract = { workspace = true }
alloy-network = { workspace = true }
alloy-signer = { workspace = true }
alloy-signer-local = { workspace = true }

# Async
tokio = { workspace = true, features = ["sync", "time"] }
futures = { workspace = true }
futures-core = { workspace = true }

# Serialization
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }
workspace-hack = { version = "0.1", path = "../../../workspace-hack" }

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
# Path-only dev-deps to break the publish cycle:
# blueprint-anvil-testing-utils -> blueprint-runner -> blueprint-client-tangle.
# Cargo strips path-only dev-deps from published manifests, so the cycle never
# reaches crates.io. See `crates/auth/Cargo.toml` for more on this trap.
blueprint-chain-setup-anvil = { path = "../../chain-setup/anvil" }
blueprint-anvil-testing-utils = { path = "../../testing-utils/anvil" }
anyhow = { workspace = true }
hex = { workspace = true }

[features]
default = ["std"]
std = [
    "blueprint-core/std",
    "blueprint-std/std",
    "blueprint-keystore/std",
    "blueprint-crypto/std",
    "alloy-primitives/std",
    "tokio/full",
]
